{% extends 'base.html.twig' %}

{% block title %} Utilisateurs {% endblock %}

{% block body %}


    <div class="container-fluid"></div>

    <div class="table-responsive">
        <table class="table">
            <thead class="blue lighten-1">
            <tr>
                <th>Username</th>
                <th>@mail</th>
                <th>Points</th>
                <th>Team</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}

                <tr>
                    <td>{{ user.username }}</td>
                    <td>{{ user.mail }}</td>
                    <td>{{ user.sexe }}</td>
                    <td>{{ user.age }}</td>
                </tr>

            {% endfor %}

            </tbody>
        </table>
    </div>
    <a href="{{ path('app_user_newUser') }}">Créer un compte</a>

    </div>
{% endblock %}
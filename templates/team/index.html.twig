{% extends 'base.html.twig' %}

{% block title %} Les Equipes {% endblock %}

{% block body %}

    <div class="table-responsive">
        <table class="table">
            <thead class="blue lighten-1">
            <tr>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for team in teams %}

                <tr>
                    <td>{{ team.nom }}</td>
                    <td><a href="{{ path('app_team_listingUsers', { 'idTeam': team.id }) }}">Liste des utilisateurs</a></td>
                </tr>

            {% endfor %}

            </tbody>
        </table>
    </div>

    <a href="{{ path('app_team_new') }}">Créer une équipe</a>

{% endblock %}
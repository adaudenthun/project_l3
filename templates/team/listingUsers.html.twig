{% extends 'base.html.twig' %}

{% block title %} Liste des utilisateurs {% endblock %}

{% block body %}


    <div class="container-fluid"></div>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>Nom</th>
            <th>prénom</th>
        </tr>
        </thead>
        <tbody>
        {% for user in listeUsers %}

            <tr>
                <td>{{ user.nom }}</td>
                <td>{{ user.prenom }}</td>
            </tr>

        {% endfor %}

        </tbody>
    </table>

    <a href="{{ path('app_team_index') }}">Retour à l'index</a>

    </div>
{% endblock %}